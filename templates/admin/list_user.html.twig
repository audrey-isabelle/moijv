{% extends "admin/layoutadmin.html.twig" %} {# remplace le require_once ... #}

{% block title %}Liste des utilisateurs{% endblock %}

{% block body %}
<h1>Bienvenue sur la liste des utilisateurs</h1>

<!-- La syntaxe ci-dessous est un commentaire twig -->
{# TODO gerenrate user list #}
<ul>
{# <?php foreach($users as $user : ?> #} <!-- boucle en php -->
{% for user in users %} <!-- idem que la boucle foreach en twig -->
    <li>
    {# <?= htmlspecialchars($user->getFirstname()); ?> #}
       {{ user.firstname }} {{ user.lastname}}
       <a href="{{ path('user_details', { id : user.id }) }}">détails</a>
    </li>    
{# <?php endforeach; ?> #}
{% endfor %}
</ul>
{% endblock %}






